"use client"

// Remove the Link import if it's not being used
import CloudinaryImage from './CloudinaryImage';

const TopDestinations = () => {
  // Component code
};

export default TopDestinations;